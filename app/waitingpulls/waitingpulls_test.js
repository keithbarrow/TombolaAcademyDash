'use strict';

describe('Tombola.Academy.Dash.view1 WaitingPulls', function() {

  beforeEach(module('Tombola.Academy.Dash.WaitingPulls'));

  describe('WaitingPulls controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var waitingPullsController = $controller('WaitingPullsController');
      expect(waitingPullsController).toBeDefined();
    }));

  });
});